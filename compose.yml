services:
  app:
    build: .
    image: clashmerge:local
    container_name: clashmerge
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
